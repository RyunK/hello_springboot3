name: Hello World!!

# 실행 조건
on:
  workflow_dispatch

# 어떤 작업을 실행할지
jobs:
  hello-world:   # 작업 이름 ID
    runs-on: ubuntu-latest   # 실행 환경
    steps:
      - name: Print hello message    # 스텝 이름
        run: echo "Hello, GitHub Actions!"    # 스텝에서 실행할 명령